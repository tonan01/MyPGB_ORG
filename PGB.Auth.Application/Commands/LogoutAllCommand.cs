using PGB.BuildingBlocks.Application.Commands;

namespace PGB.Auth.Application.Commands
{
    #region Logout All Command
    /// <summary>
    /// Command để logout khỏi tất cả devices
    /// </summary>
    public class LogoutAllCommand : BaseCommand
    {
        // UserId được lấy từ authenticated user (BaseCommand.UserId)
    }
    #endregion
}